{
  "name": "words",
  "version": "0.0.0",
  "private": true,
  "devDependencies": {
    "prettier": "2.0.5"
  },
  "scripts": {
    "postinstall": "gem install jekyll bundler --conservative && bundle install && gem uninstall eventmachine --force && gem install eventmachine --platform ruby",
    "format": "prettier --write **/*.{md,scss}",
    "start": "bundle exec jekyll serve --livereload"
  },
  "prettier": {
    "printWidth": 60,
    "trailingComma": "none",
    "arrowParens": "avoid"
  }
}
